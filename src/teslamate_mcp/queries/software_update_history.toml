name = "get_software_update_history"
description = "Get the software update history for each car. Tracks firmware updates and version changes over time."

[filters]
car_column = "u.car_id"
date_column = "u.start_date"
